apiVersion: v1
kind: Namespace
metadata:
  name: ns-chatbot-ui
---
apiVersion: v1
kind: Secret
metadata:
  namespace: ns-chatbot-ui
  name: secret-chatbot-ui
type: Opaque
data:
  OPENAI_API_KEY: <base64 encoded key> # TODO
---
apiVersion: apps/v1
kind: Deployment
metadata:
  namespace: ns-chatbot-ui
  name: chatbot-ui
  labels:
    app: chatbot-ui
spec:
  replicas: 1
  selector:
    matchLabels:
      app: chatbot-ui
  template:
    metadata:
      labels:
        app: chatbot-ui
    spec:
      containers:
        - name: chatbot-ui
          image: ghcr.io/pichuang/chatbot-ui:devdaysasia2023
          resources: {}
          ports:
            - containerPort: 3000
          env:
            - name: OPENAI_API_KEY
              valueFrom:
                secretKeyRef:
                  name: secret-chatbot-ui
                  key: OPENAI_API_KEY
---
kind: Service
apiVersion: v1
metadata:
  namespace: ns-chatbot-ui
  name: svc-chatbot-ui
  labels:
    app: chatbot-ui
spec:
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 3000
  selector:
    app: chatbot-ui
  type: ClusterIP